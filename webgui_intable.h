#ifndef HAVE_INTABLE_H
#define HAVE_INTABLE_H

#include "in.h"

#include <string>

std::string table_fromto(in*, double, double);	// Timestamps
std::string table_fromto_h(in*, double, double);	// Formatted date/time


#endif // HAVE_INTABLE_H
